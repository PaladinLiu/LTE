<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myj2ee.dao.UserDao">
    <select id="getUserByUsername" resultMap="myUser">
        select * from tbUser where username = #{username}
    </select>

    <select id="getAllUser" resultMap="myUser">
        select * from tbUser
    </select>

    <insert id="addUser">
        insert into tbUser(username, password, isAdmin) values(#{username},#{password},#{admin})
    </insert>

    <delete id="deleteUser">
        delete from tbUser where id = #{id}
    </delete>
    
    <resultMap id="myUser" type="myj2ee.bean.User">
        <result property="id" column="id"></result>
        <result property="username" column="username"></result>
        <result property="password" column="password"></result>
        <result property="admin" column="isAdmin"></result>
    </resultMap>
</mapper>


