<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myj2ee.dao.C2i3Dao">
    <insert id="addC2i3">
        insert into tbc2i3(acell, bcell, ccell)
        values (#{acell}, #{bcell}, #{ccell})
    </insert>

    <delete id="deleteC2i3">
        delete from tbc2i3 where acell = #{acell} and bcell = #{bcell} and ccell = #{ccell}
    </delete>

    <select id="getAllC2i3" resultMap="infoC2i3">
        select * from tbc2i3
    </select>

    <select id="getC2i3" resultMap="infoC2i3">
        select * from tbc2i3 where acell = #{acell} and bcell = #{bcell} and ccell = #{ccell}
    </select>

    <resultMap id="infoC2i3" type="myj2ee.bean.C2i3">
        <result property="acell" column="acell"/>
        <result property="bcell" column="bcell"/>
        <result property="ccell" column="ccell"/>
    </resultMap>
</mapper>