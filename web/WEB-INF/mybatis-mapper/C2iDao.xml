<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myj2ee.dao.C2iDao">
    <insert id="addC2i">
        insert into tbc2i
        <foreach collection="columns" item="column" separator="," open="(" close=")">
            ${column}
        </foreach>
        values
        <if test = "c2is.size > 0">
            <foreach collection="c2is" item="c2i" separator=",">
                <foreach collection="c2i" item="attribute" separator="," open="(" close=")">
                    #{attribute}
                </foreach>
            </foreach>
        </if>
        on duplicate KEY UPDATE
        <foreach collection="columns" item="column" separator=",">
            ${column} = VALUES(${column})
        </foreach>

    </insert>
    
    <select id="getAllC2i" resultType="java.util.Map" resultSetType="FORWARD_ONLY" fetchSize="-2147483648">
        select * from tbc2i
    </select>

    <select id="getAllColumnName" parameterType="java.lang.String" resultType="java.lang.String">
        select COLUMN_NAME
        from information_schema.COLUMNS
        WHERE TABLE_NAME = 'tbC2i'
        order by ORDINAL_POSITION
    </select>

    <select id="getRowCount" resultType="java.lang.Integer">
        select count(*) from tbc2i
    </select>

    <select id="get50Row" resultType="java.util.Map">
        select * from tbc2i limit #{start}, 50
    </select>

</mapper>